package exercices.exercice01;

public class ouais {
    public static void main(String[] args) {
               // Exemple de tableau d'entiers
               public static int compterOccurrences(int[] tableau, int valeurRecherchee) {
                public static int trouverPlusPetiteValeur(int[] tableau) {
                    // On suppose que le premier élément du tableau est la plus petite valeur
                    int min = tableau[0];
            
                    // Parcours du tableau pour trouver la plus petite valeur
                    for (int i = 1; i < tableau.length; i++) {
                        if (tableau[i] < min) {
                            min = tableau[i]; // On met à jour la plus petite valeur
      }